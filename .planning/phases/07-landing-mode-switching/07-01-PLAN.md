---
phase: 07-landing-mode-switching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Landing/Landing.ts
  - src/components/Landing/Landing.css
  - src/styles/variables.css
autonomous: true

must_haves:
  truths:
    - "Landing displays vertical split with website on left, game on right"
    - "Landing shows intro text about Sam Reynolds (conservation and AI research)"
    - "Each half has title, tagline, and Enter button"
    - "Layout stacks vertically on mobile"
  artifacts:
    - path: "src/components/Landing/Landing.ts"
      provides: "Landing component with renderLanding() export"
      exports: ["renderLanding"]
    - path: "src/components/Landing/Landing.css"
      provides: "Split screen layout styles"
      contains: ".landing"
    - path: "src/styles/variables.css"
      provides: "Transition tokens for landing"
      contains: "--transition-expand"
  key_links:
    - from: "src/components/Landing/Landing.ts"
      to: "src/content/about.json"
      via: "import for intro text"
      pattern: "import.*about"
---

<objective>
Create the landing page component with vertical split layout showing website and game mode choices.

Purpose: Establish the visual foundation for mode selection that visitors see on first visit
Output: Landing.ts component with CSS, ready for route integration
</objective>

<execution_context>
@/home/sam/.claude/get-shit-done/workflows/execute-plan.md
@/home/sam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-landing-mode-switching/07-CONTEXT.md
@.planning/phases/07-landing-mode-switching/07-RESEARCH.md
@src/styles/variables.css
@src/content/about.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add landing transition tokens to variables.css</name>
  <files>src/styles/variables.css</files>
  <action>
Add these CSS custom properties to :root in variables.css:

```css
/* Landing transitions */
--transition-expand: 250ms cubic-bezier(0.65, 0, 0.35, 1);
--transition-fade: 200ms ease-out;
```

Place them after the existing --transition-normal line.
  </action>
  <verify>grep "transition-expand" src/styles/variables.css</verify>
  <done>Transition tokens exist in variables.css</done>
</task>

<task type="auto">
  <name>Task 2: Create Landing component with split layout</name>
  <files>src/components/Landing/Landing.ts, src/components/Landing/Landing.css</files>
  <action>
Create Landing.ts that renders:

1. **Full-viewport container** with Flexbox row layout (flex-direction: column on mobile < 768px)

2. **Left half (website mode):**
   - Title: "Traditional Website"
   - Tagline: "Browse publications, talks, and research in a clean academic layout"
   - Placeholder div for website preview (320x240, gray background with text "Website Preview")
   - Button: "Enter Website" with data-mode="website"

3. **Right half (game mode):**
   - Title: "Explore Campus"
   - Tagline: "Walk through a pixel-art Cambridge campus and discover content"
   - Placeholder div for game preview (320x240, gray background with text "Game Preview") - will be replaced by Plan 02
   - Button: "Enter Game" with data-mode="game"

4. **Intro section** centered above the split (or below on mobile):
   - Use brief bio from about.json: "Conservation biologist and AI researcher at Cambridge..."
   - Keep it to 1-2 sentences max

5. **Footer area** with placeholder for skip links (just the container, actual links in Plan 03)

CSS requirements (Landing.css):
- .landing: display: flex; flex-direction: row; min-height: 100vh;
- .landing-half: flex: 1; display: flex; flex-direction: column; justify-content: center; align-items: center; padding: var(--space-8);
- .landing-half--website: background: var(--color-gray-50);
- .landing-half--game: background: var(--color-gray-100);
- .landing-preview: width: 320px; height: 240px; border-radius: var(--radius-md); overflow: hidden;
- .landing-button: styled button with hover states (use existing accent color)
- Mobile breakpoint @media (max-width: 768px): flex-direction: column; min-height: 50vh per half
- Reduced motion: @media (prefers-reduced-motion: reduce) for transitions

Export function renderLanding() that returns the HTML string.
Do NOT import or wire up the CSS - that will be done in main.ts in Plan 03.
  </action>
  <verify>
ls src/components/Landing/Landing.ts src/components/Landing/Landing.css
grep "export.*renderLanding" src/components/Landing/Landing.ts
grep ".landing-half" src/components/Landing/Landing.css
  </verify>
  <done>
Landing component exists with:
- renderLanding() exported
- Vertical split layout with website/game halves
- Intro text from about.json
- Placeholder preview areas
- Mobile responsive CSS
  </done>
</task>

</tasks>

<verification>
1. Landing.ts exports renderLanding function
2. Landing.css contains split screen layout styles
3. variables.css has landing transition tokens
4. Mobile breakpoint switches to column layout
5. prefers-reduced-motion media query present
</verification>

<success_criteria>
- Landing component renders split layout with both mode choices
- CSS implements Flexbox split with mobile stacking
- Transition tokens available for animations
- Component ready for route integration (Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/07-landing-mode-switching/07-01-SUMMARY.md`
</output>

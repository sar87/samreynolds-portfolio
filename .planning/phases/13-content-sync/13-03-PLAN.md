---
phase: 13-content-sync
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - data/about.json
  - data/research.json
autonomous: true

must_haves:
  truths:
    - "About section shows AI + Conservation focus as leading emphasis"
    - "Email is updated to sar87@cam.ac.uk"
    - "Bio matches samreynolds.org tone and length"
    - "Research section shows 2 main areas (Conservation+AI, PhD invasive species)"
  artifacts:
    - path: "data/about.json"
      provides: "Updated bio with AI evidence synthesis focus"
      contains: "sar87@cam.ac.uk"
    - path: "data/research.json"
      provides: "Simplified research topics (2 areas)"
      contains: "evidence synthesis"
  key_links:
    - from: "data/about.json"
      to: "src/pages/HomePage.ts"
      via: "getAbout() in src/lib/content.ts"
      pattern: "about.*bio.*email"
    - from: "data/research.json"
      to: "src/pages/HomePage.ts"
      via: "getResearchTopics() in src/lib/content.ts"
      pattern: "researchTopics.*name.*description"
---

<objective>
Update about/bio and research content from samreynolds.org

Purpose: Reflect current focus on AI evidence synthesis and simplify research structure
Output: Updated about.json and research.json matching samreynolds.org
</objective>

<execution_context>
@/home/sam/.claude/get-shit-done/workflows/execute-plan.md
@/home/sam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-content-sync/13-CONTEXT.md
@.planning/phases/13-content-sync/13-RESEARCH.md
@data/about.json
@data/research.json
@src/types/content.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update about.json with samreynolds.org bio</name>
  <files>data/about.json</files>
  <action>
First, use WebFetch to get bio content from https://samreynolds.org/ (about page or homepage bio).

Then update data/about.json to match samreynolds.org:

Required updates (from CONTEXT.md and RESEARCH.md):
1. **Email**: Change to "sar87@cam.ac.uk"
2. **Bio**: Match samreynolds.org tone and length
   - Lead with AI + Conservation focus (per CONTEXT.md)
   - Emphasize evidence synthesis work
3. **Education**: Verify matches samreynolds.org
   - PhD Zoology Cambridge (year)
   - MSc + BSc Warwick
4. **Links**: Update if different on samreynolds.org

Follow existing schema:
```typescript
interface About {
  name: string;
  title: string;
  affiliation: string;
  email: string;
  bio: string[];  // Array of paragraph strings
  education: Education[];
  links?: AboutLinks;
}
```

IMPORTANT:
- Match samreynolds.org tone exactly
- Bio should lead with AI evidence synthesis focus
- Keep bio length similar to samreynolds.org (not artificially expanded)
  </action>
  <verify>
Run: `node -e "const a = require('./data/about.json'); console.log(a.email, a.bio[0].substring(0, 50))"`
Expected: Shows sar87@cam.ac.uk and first 50 chars of bio
  </verify>
  <done>
- Email is sar87@cam.ac.uk
- Bio leads with AI + Conservation / evidence synthesis focus
- Bio tone and length match samreynolds.org
- Education matches samreynolds.org records
  </done>
</task>

<task type="auto">
  <name>Task 2: Simplify research.json to match samreynolds.org structure</name>
  <files>data/research.json</files>
  <action>
First, use WebFetch to understand how samreynolds.org organizes research areas.

Per RESEARCH.md, samreynolds.org has 2 main areas vs current 6 topics:
1. **Conservation + AI** (evidence synthesis, LLMs, horizon scanning)
2. **PhD Research** (invasive species, freshwater ecology)

Replace the entire researchTopics array with 2 research areas:

```json
{
  "researchTopics": [
    {
      "id": "ai-conservation",
      "name": "[Name from samreynolds.org]",
      "description": "[Description from samreynolds.org - AI evidence synthesis, LLMs for conservation, etc.]",
      "relatedPublications": ["relevant-pub-ids"]
    },
    {
      "id": "invasive-species",
      "name": "[Name from samreynolds.org - PhD work]",
      "description": "[Description from samreynolds.org - freshwater invasives, crayfish, etc.]",
      "relatedPublications": ["relevant-pub-ids"]
    }
  ]
}
```

Follow existing schema:
```typescript
interface ResearchTopic {
  id: string;
  name: string;
  description: string;
  relatedPublications?: string[];
}
```

NOTE: relatedPublications should reference IDs from the updated publications.json (from Plan 01). Use IDs that will exist after Plan 01 completes. If uncertain, omit relatedPublications or use obvious matches like "ai-enabled-evidence-synthesis".

IMPORTANT:
- Complete replacement, not merge
- Match structure and tone from samreynolds.org
- 2 topics, not 6
  </action>
  <verify>
Run: `node -e "const r = require('./data/research.json'); console.log(r.researchTopics.length, r.researchTopics.map(t => t.name))"`
Expected: Shows 2 and the topic names
  </verify>
  <done>
- research.json contains exactly 2 research topics
- First topic covers AI/Conservation/Evidence Synthesis
- Second topic covers PhD work on invasive species
- Descriptions match samreynolds.org content
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` - site builds without errors
2. Navigate to About section - shows updated bio with AI focus
3. Email link shows sar87@cam.ac.uk
4. Navigate to Research section - shows 2 topics (not 6)
5. Research descriptions match samreynolds.org
</verification>

<success_criteria>
- data/about.json has email sar87@cam.ac.uk
- Bio leads with AI evidence synthesis focus
- data/research.json has 2 topics matching samreynolds.org structure
- No placeholder content remains in either file
</success_criteria>

<output>
After completion, create `.planning/phases/13-content-sync/13-03-SUMMARY.md`
</output>
